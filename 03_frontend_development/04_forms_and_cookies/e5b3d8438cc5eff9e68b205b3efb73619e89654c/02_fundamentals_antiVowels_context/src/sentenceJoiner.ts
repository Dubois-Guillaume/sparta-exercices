// Copy your function `sentenceJoiner` from the first exercise here
export const sentenceJoiner = (sentenceArray: string[]): string => {
  // Code the sentenceJoiner function here
  const newString = sentenceArray.join(" ");
  return newString;
};
